{
  "name": "@sia/contracts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "openapi-typescript openapi.json -o src/generated.ts",
    "check": "pnpm generate && git diff --exit-code -- openapi.json src/generated.ts",
    "lint": "oxlint src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "typescript": "~6.0.2"
  }
}
